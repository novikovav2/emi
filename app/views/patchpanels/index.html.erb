<%= link_to new_patchpanel_path, class: "btn btn-outline-secondary btn-sm" do %>
  <i class="fas fa-plus"></i> Добавить
<% end %>

<a class="btn btn-light btn-sm" data-toggle="collapse" href="#Filter" role="button"
   aria-expanded="false" aria-controls="collapseExample">
  <i class="fas fa-angle-down"></i> Фильтр
</a>

<%= render 'filter' %>

<div class="row mt-3">
  <div class="col-xl-12">
    <div class="card">
      <div class="card-body">
        <%= render 'index_table', patchpanels: @patchpanels %>
      </div>
    </div>
  </div>
</div>

